# Evidências — Item 1.1.C1 (CODEX)

## Mudanças executadas
- Bootstrap do monorepo com `turbo.json` e scripts raiz em `package.json`.
- Criação dos apps `apps/web`, `apps/api` e `apps/worker` com `package.json`, `tsconfig.json` e `src/index.ts`.
- Criação de `tsconfig.base.json` compartilhado para compilação TypeScript.

## Checagens executadas
1. `npm install`
   - Resultado: falhou com `403 Forbidden` ao acessar `registry.npmjs.org` no ambiente.

## Risco / Pendência
- Dependências não instaladas no ambiente atual, portanto build/typecheck/lint não puderam ser executados localmente nesta rodada.
