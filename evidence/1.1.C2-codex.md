# Evidências — Item 1.1.C2 (CODEX)

## Mudanças executadas
- Ativei `strict: true` no `tsconfig.base.json`.
- Configurei `baseUrl` e `paths` para importação entre packages:
  - `@birthhub/agents-core`
  - `@birthhub/agents-registry`
  - `@birthhub/agent-pack-corporate-v1`
- Criei estrutura mínima dos packages `agents-core`, `agents-registry` e `agent-packs/corporate-v1` com código real de importação cruzada.
- Atualizei workspaces no `package.json` raiz para incluir `packages/*` e `packages/agent-packs/*`.

## Checagens executadas
1. `python -m json.tool package.json`
2. `python -m json.tool tsconfig.base.json`
3. `rg -n "@birthhub/agents-core|@birthhub/agents-registry" packages`

## Risco / Pendência
- `npm install` continua bloqueado por política de rede (403), impedindo validação via `tsc` nesta rodada.
- Dependência operacional: item `1.1.C1` ainda aguarda validação formal do JULES.
